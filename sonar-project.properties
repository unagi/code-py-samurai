sonar.projectKey=unagi_code-py-samurai
sonar.organization=unagi

# Coverage is tracked by Codecov; disable SonarCloud coverage to avoid duplicate reporting
sonar.coverage.exclusions=**/*

# Static reference content is intentionally repetitive (localized doc data tables)
sonar.cpd.exclusions=src/web/reference/reference-data.ts

# Exclude non-application directories from analysis
sonar.exclusions=tools/**
